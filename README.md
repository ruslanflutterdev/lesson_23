# 🚦 Traffic Rules Quiz (Lesson 23)

**Traffic Rules Quiz** — это обучающее мобильное приложение на Flutter для проверки знаний правил дорожного движения. В отличие от простых тестов, это приложение включает в себя полный цикл прохождения викторины с финальным экраном результатов.

## ✨ Основные возможности

* **🏠 Главное меню**: Стартовый экран с красивым оформлением и кнопкой начала.
* **❓ Прохождение теста**: Экран с вопросами и вариантами ответов.
* **📊 Экран результатов**:
    * После завершения теста отображается итоговый результат.
    * Детальный список ответов (виджет `QuestionResult`), показывающий, где пользователь ошибся.
* **🎨 Кастомный дизайн**: Использование нестандартных контейнеров (`PannedContainer`, `DecoratedContainer`) для уникального стиля UI.
* **💾 Локальные данные**: Вопросы хранятся внутри приложения (`question_data.dart`), интернет не требуется.

## 🛠 Технологический стек

* **Фреймворк**: [Flutter](https://flutter.dev/) (Dart)
* **Архитектура**: Простая навигация между экранами.
* **Данные**: Использование моделей (`QuestionModel`, `QuestionResultModel`) для структурирования информации.

## 📂 Структура проекта

Проект имеет четкое разделение на слои (данные, модели, экраны, виджеты):

```text
lib/
├── data/
│   └── question_data.dart       # Список вопросов и ответов (Mock Data)
├── models/
│   ├── question_model.dart      # Модель вопроса
│   └── question_result_model.dart # Модель результата ответа
├── screens/
│   ├── home_screen.dart         # Главный экран
│   ├── question_screen.dart     # Экран викторины
│   └── results_screen.dart      # Экран итогов (Список правильных/неправильных ответов)
├── widgets/
│   ├── decorated_container.dart # Стилизованный контейнер
│   ├── panned_container.dart    # Контейнер с особым оформлением
│   └── question_result.dart     # Виджет строки результата
└── main.dart                    # Точка входа
